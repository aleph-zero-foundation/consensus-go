{
	"NParents": 128,

	"PrimeOnly": true,

	"CanSkipLevel": true, 

	"CreateDelay": 0.1,

	"StepSize": 0.0,

    "Sync": [{
        "Type": "fetch",
        "Params": {
            "nIn": "20",
            "nOut": "40",
            "timeout": "2",
            "retryingFallback": "fetch",
            "retryingInterval": "0.3"
            },
        "Fallback": "retrying"
        }, {
        "Type": "multicast",
        "Params": {
            "mcType": "tcp",
            "timeout": "2"
        },
        "Fallback": "retrying"
    }],

	"Setup": "coin",

	"SyncSetup": [],

	"Txpu": 1,

	"LevelLimit": 50,

	"VotingLevel": 3,

	"PiDeltaLevel": 12,

	"AddShares": 0,

	"LogLevel": 0,

	"LogBuffer": 100000,

	"LogMemInterval": 10,

	"LogHuman": false,

    "OrderStartLevel": 0,

	"CRPFixedPrefix": 5
}
